cd ~/Desktop/Projects/workout-app
cat > netlify.toml << 'EOF'
[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
EOF
